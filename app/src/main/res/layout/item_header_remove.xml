<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="item"
            type="com.example.iikoapi.entities.ParentRemove" />

        <variable
            name="action"
            type="com.example.iikoapi.utils.OnHeaderClickListener" />

        <variable
            name="position"
            type="java.lang.Integer" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/background_ripple_23"
        android:backgroundTint="@color/lightGrey"
        app:onClickListener="@{view -> action.onClick(view, position, arrow)}">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="najks"
            android:gravity="center"
            android:textSize="17sp"
            android:textColor="#000"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginVertical="16dp"
            android:text="@{item.parent}"/>

        <ImageView
            android:id="@+id/arrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            android:src="@drawable/ic_expandable_arrow"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="16dp"
            app:arrow="@{item.state}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>