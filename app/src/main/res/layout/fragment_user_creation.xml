<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.example.iikoapi.profile.Authorisation"/>
        <import type="android.view.View" />

        <variable
            name="action"
            type="android.view.View.OnClickListener" />

        <variable
            name="type"
            type="com.example.iikoapi.profile.Authorisation" />


    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolBar"
            style="@style/ToolBar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:elevation="4dp"
            app:layout_constraintTop_toTopOf="parent"
            app:title="@string/user_creation_fragment_title" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintTop_toBottomOf="@id/toolBar">

            <EditText
                android:id="@+id/nameEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="16dp"
                android:visibility="@{type instanceof Authorisation.Login ? View.GONE : View.VISIBLE}"
                android:layout_marginTop="32dp"
                android:hint="@string/user_creation_fragment_name" />

            <EditText
                android:id="@+id/phoneEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="16dp"
                android:layout_marginTop="32dp"
                android:hint="@string/user_creation_fragment_phone"
                android:inputType="phone" />

            <EditText
                android:id="@+id/passwordEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="16dp"
                android:layout_marginTop="32dp"
                android:hint="@string/user_creation_fragment_password" />

        </LinearLayout>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/payGoToMenu"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/background_ripple_23"
            android:backgroundTint="@color/main_yelow"
            android:onClick="@{action}"
            android:text="@{type instanceof Authorisation.Registration ? @string/user_creation_fragment_registration : @string/profile_fragment_login}"
            android:textAllCaps="false"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>